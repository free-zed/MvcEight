@model IEnumerable<MvcEight.Models.Exam>

@*@{
    ViewBag.Title = "Index";
}*@

<style>
    table {
        border-collapse: collapse;
        width: 100%;
    }

    th, td {
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #90EE90;
    }

    th {
        background-color: #4CAF50;
        color: white;
    }
</style>

<h2 style="text-align:center;">Exam Notice</h2>
<br />


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExamDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExamSubject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExamSyllabus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExamDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExamSubject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExamSyllabus)
        </td>
        <td>
            @Html.ActionLink("Change", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Remove", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>

<hr />

<p style="text-align:center;">
    @Html.ActionLink("Add Exam Notice", "Create")
</p>